# Ralph Runner Instructions

You are running one opt-in Ralph iteration for `playground`.

## Run Context

- Ralph run directory: `.ralph/cross-agent-runtime-contract`
- PRD title: `Tighten cross-agent runtime contract for rules, hooks, and on-demand skills`
- PRD branch: `(not specified)`
- Current git branch: `refactor_agentic-setup`
- Active story: `STORY-3 - Define the supported cross-agent hook subset`
- Active story status: `pending`

## Files To Read First

1. `AGENTS.md`
2. Relevant `.agents/rules/` files
3. `.ralph/cross-agent-runtime-contract/prd.json`
4. `.ralph/cross-agent-runtime-contract/progress.txt`
5. Any closer `AGENTS.md` files in directories you edit

## Story Notes

Clarify which hook events are safe to treat as shared across Codex, Claude, and Copilot CLI. The likely shared minimum is startup/session-start plus user-prompt submission, with richer pre/post tool hooks treated as runtime-specific extensions.

## Story Queue Snapshot

- STORY-1: Research first-party runtime surfaces and define the shared contract [status=done, priority=high]
- STORY-2: Tighten runtime references into compact contract-style docs [status=done, priority=high]
- STORY-3: Define the supported cross-agent hook subset [status=pending, priority=high]
- STORY-4: Add a Copilot CLI hook adapter plan without forking hook logic [status=pending, priority=medium]
- STORY-5: Document runtime-specific skill mapping strategy [status=pending, priority=medium]

## Recent Progress Tail

```text
- Shared hook logic should live in `.agents/hooks/`, but runtime adapter
  registration files remain runtime-specific.
- Repo-owned skills should stay in `.skills/` and be loaded on demand via
  `pnpm skills:list|search|read`.

## Run Log

- 2026-04-29: Reviewed first-party runtime docs and tightened the contract:
  Codex uses repository `AGENTS.md` guidance and local config; Claude supports
  project slash commands and hooks via `.claude/*`; Copilot CLI supports
  repository custom instructions and `.github/hooks/*.json`.
- 2026-04-29: Added `.agents/references/agent-runtimes/shared-contract.md` and
  tightened the runtime reference folder into compact contract-style files with
  source links.
- 2026-04-29: Added a clearer README with TOC and read-more pointers so the
  runtime knowledge base works as a small sitemap instead of a loose note dump.
```

## Required Working Style

- Implement only the single active story for this iteration.
- Use repo-native conventions: `pnpm`, workspace-scoped checks first, and
  `jcodemunch` first for code navigation, with `ai-context-engine`
  (`@astrograph`) kept available as the secondary path until the repo is ready
  to switch fully.
- Keep the worktree coherent and avoid unrelated cleanup.
- Update `.ralph/cross-agent-runtime-contract/progress.txt` by appending a short iteration log with:
  story completed or blocked
  files changed
  checks run
  reusable learnings or gotchas
- Leave a short next-step note when partial work remains so the next iteration
  can resume without rediscovery.
- Update `.ralph/cross-agent-runtime-contract/prd.json` only if the story is fully complete and the
  listed checks passed.
- Add reusable directory-specific learnings to nearby `AGENTS.md` files only if
  they are truly durable and worth loading in future sessions.

## Checks To Run

- pnpm agents:check
- pnpm lint:md

## Commit Policy

Do not commit automatically.
Leave the worktree ready for human review after updating `prd.json` and `progress.txt`.

## Guardrails

- Do not switch branches automatically.
- Do not bypass approvals or sandboxing.
- Do not mark the story passed if verification is incomplete.
- If blocked, leave a clear blocker note in `progress.txt` instead of guessing.
