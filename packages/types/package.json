{
  "name": "@playground/types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared micro frontend contracts for the todo domain",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@playground/eslint-config": "workspace:*",
    "@playground/tsconfig": "workspace:*",
    "typescript": "^5.7.0",
    "eslint": "^8.57.1"
  }
}
