# Microfrontend Cleanup Implementation Plan

> **Status: Superseded.** MFE scope narrowed to todo-app only. uplink-game is now a direct host library (`UplinkGameCanvas`). All work landed on main via the EFA refactor (commit `f40413c`).

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reduce 5 apps to 3, replace Mantine with Tailwind + shadcn/ui, enforce dark mode only, and give the host a full-width header + sidebar navigation.

**Architecture:** Delete `compendium`, `todo-input`, `todo-list`, `todo-stats`. Create `todo-app` — a single Vite MFE that merges all todo UI with internal localStorage state (no bridge). Rewrite `host` with Tailwind + shadcn: full-width header, left sidebar, content area. Host loads `todo-app` at runtime via `/* webpackIgnore: true */` dynamic import. README viewer is a Next.js server component page inside host.

**Tech Stack:** Next.js 14 App Router, Tailwind CSS 3, shadcn/ui, Vite 5, React 18, TypeScript 5.7, pnpm workspaces, Turborepo, vitest (todo-app tests), vite-plugin-css-injected-by-js (CSS bundled into remoteEntry.js)

---

## File Map

### Deleted
- `apps/compendium/` — entire directory
- `apps/todo-input/` — entire directory
- `apps/todo-list/` — entire directory
- `apps/todo-stats/` — entire directory
- `apps/host/src/components/remote-slot.tsx`
- `apps/host/src/components/todo-debug-panel.tsx`
- `apps/host/src/components/todo-orchestrator.tsx`
- `apps/host/src/lib/todo-bridge.ts`
- `apps/host/src/lib/injected-remotes.ts`
- `apps/host/src/lib/remotes.ts`

### Created
- `apps/todo-app/package.json`
- `apps/todo-app/tsconfig.json`
- `apps/todo-app/vite.config.ts`
- `apps/todo-app/postcss.config.cjs`
- `apps/todo-app/tailwind.config.ts`
- `apps/todo-app/src/index.css`
- `apps/todo-app/src/store.ts`
- `apps/todo-app/src/store.test.ts`
- `apps/todo-app/src/components/TodoInput.tsx`
- `apps/todo-app/src/components/TodoList.tsx`
- `apps/todo-app/src/components/TodoStats.tsx`
- `apps/todo-app/src/App.tsx`
- `apps/todo-app/src/remote-entry.tsx`
- `apps/host/tailwind.config.ts`
- `apps/host/postcss.config.cjs`
- `apps/host/components.json`
- `apps/host/src/lib/utils.ts`
- `apps/host/src/components/ui/` (shadcn — generated by CLI)
- `apps/host/src/components/header.tsx`
- `apps/host/src/components/sidebar.tsx`
- `apps/host/src/components/mfe-frame.tsx`
- `apps/host/src/app/todo/page.tsx`
- `apps/host/src/app/readme/page.tsx`

### Modified
- `apps/host/package.json`
- `apps/host/next.config.mjs`
- `apps/host/src/app/layout.tsx`
- `apps/host/src/app/globals.css`
- `apps/host/src/app/page.tsx`
- `apps/host/src/app/error.tsx`
- `packages/types/src/index.ts`

---

## Task 1: Delete obsolete apps

**Files:**
- Delete: `apps/compendium/`, `apps/todo-input/`, `apps/todo-list/`, `apps/todo-stats/`

- [ ] **Step 1: Delete the four obsolete app directories**

```bash
rm -rf apps/compendium apps/todo-input apps/todo-list apps/todo-stats
```

- [ ] **Step 2: Verify they're gone**

```bash
ls apps/
```

Expected output:
```
claude-agents  host
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: delete obsolete apps (compendium, todo-input, todo-list, todo-stats)"
```

---

## Task 2: Simplify packages/types — remove bridge types

**Files:**
- Modify: `packages/types/src/index.ts`

- [ ] **Step 1: Replace the entire file with just the Todo type**

```typescript
// packages/types/src/index.ts
export type Todo = {
  id: string;
  title: string;
  completed: boolean;
};
```

- [ ] **Step 2: Verify TypeScript is happy**

```bash
pnpm --filter @playground/types type-check
```

Expected: no errors (or command exits 0)

- [ ] **Step 3: Commit**

```bash
git add packages/types/src/index.ts
git commit -m "chore(types): remove bridge types, keep only Todo"
```

---

## Task 3: Update host package.json and next.config.mjs

**Files:**
- Modify: `apps/host/package.json`
- Modify: `apps/host/next.config.mjs`

- [ ] **Step 1: Replace apps/host/package.json**

```json
{
  "name": "@playground/host",
  "version": "0.0.1",
  "private": true,
  "description": "Playground shell — app launcher with header, sidebar, and micro frontend routing",
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@playground/types": "workspace:*",
    "highlight.js": "^11.10.0",
    "next": "14.2.33",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-markdown": "^9.0.1",
    "rehype-highlight": "^7.0.0"
  },
  "devDependencies": {
    "@playground/tsconfig": "workspace:*",
    "@tailwindcss/typography": "^0.5.15",
    "@types/node": "^22.10.1",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "autoprefixer": "^10.4.20",
    "eslint": "^8.57.1",
    "eslint-config-next": "14.2.33",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.0"
  }
}
```

- [ ] **Step 2: Replace apps/host/next.config.mjs**

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    typedRoutes: true
  }
};

export default nextConfig;
```

- [ ] **Step 3: Install deps from repo root**

```bash
pnpm install
```

Expected: lockfile updated, no ERR_PNPM_WORKSPACE_PKG_NOT_FOUND errors

- [ ] **Step 4: Commit**

```bash
git add apps/host/package.json apps/host/next.config.mjs pnpm-lock.yaml
git commit -m "chore(host): remove Mantine + old MFE deps, add tailwind + react-markdown"
```

---

## Task 4: Scaffold apps/todo-app

**Files:**
- Create: `apps/todo-app/package.json`
- Create: `apps/todo-app/tsconfig.json`
- Create: `apps/todo-app/vite.config.ts`
- Create: `apps/todo-app/postcss.config.cjs`
- Create: `apps/todo-app/tailwind.config.ts`

- [ ] **Step 1: Create apps/todo-app/package.json**

```json
{
  "name": "@playground/todo-app",
  "version": "0.0.1",
  "private": true,
  "description": "Todo micro frontend — input, list, and stats in one self-contained Vite app",
  "type": "module",
  "scripts": {
    "dev": "vite build --mode development && concurrently -k -n build,preview \"vite build --watch --mode development\" \"vite preview --port 3101 --strictPort\"",
    "build": "vite build",
    "preview": "vite preview --port 3101 --strictPort",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@playground/types": "workspace:*",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@playground/tsconfig": "workspace:*",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "concurrently": "^9.2.1",
    "eslint": "^8.57.1",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.0",
    "vite": "^5.4.11",
    "vite-plugin-css-injected-by-js": "^3.5.2",
    "vitest": "^2.1.8"
  }
}
```

- [ ] **Step 2: Create apps/todo-app/tsconfig.json**

```json
{
  "extends": "@playground/tsconfig/base.json",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "noEmit": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
```

- [ ] **Step 3: Create apps/todo-app/vite.config.ts**

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import cssInjectedByJsPlugin from 'vite-plugin-css-injected-by-js';

export default defineConfig({
  plugins: [react(), cssInjectedByJsPlugin()],
  server: {
    cors: true,
    headers: { 'Access-Control-Allow-Origin': '*' },
  },
  preview: {
    cors: true,
    headers: { 'Access-Control-Allow-Origin': '*' },
  },
  build: {
    lib: {
      entry: 'src/remote-entry.tsx',
      formats: ['es'],
      fileName: () => 'remoteEntry.js',
    },
    target: 'es2022',
  },
  test: {
    environment: 'jsdom',
    globals: true,
  },
});
```

- [ ] **Step 4: Create apps/todo-app/postcss.config.cjs**

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 5: Create apps/todo-app/tailwind.config.ts**

```typescript
import type { Config } from 'tailwindcss';

export default {
  content: ['./src/**/*.{ts,tsx}'],
  darkMode: 'class',
  theme: {
    extend: {},
  },
  plugins: [],
} satisfies Config;
```

- [ ] **Step 6: Install from repo root (registers todo-app in workspace)**

```bash
pnpm install
```

Expected: `@playground/todo-app` appears in workspace

- [ ] **Step 7: Commit**

```bash
git add apps/todo-app/ pnpm-lock.yaml
git commit -m "feat(todo-app): scaffold Vite MFE package with Tailwind config"
```

---

## Task 5: Implement todo-app store (TDD)

**Files:**
- Create: `apps/todo-app/src/store.ts`
- Create: `apps/todo-app/src/store.test.ts`

- [ ] **Step 1: Write the failing tests first**

Create `apps/todo-app/src/store.test.ts`:

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { getTodos, addTodo, toggleTodo, deleteTodo, subscribe, _resetForTest } from './store';

beforeEach(() => {
  localStorage.clear();
  _resetForTest();
});

describe('addTodo', () => {
  it('adds a todo with the given title', () => {
    addTodo('Buy milk');
    expect(getTodos()).toHaveLength(1);
    expect(getTodos()[0].title).toBe('Buy milk');
    expect(getTodos()[0].completed).toBe(false);
    expect(typeof getTodos()[0].id).toBe('string');
  });

  it('trims whitespace from title', () => {
    addTodo('  Buy milk  ');
    expect(getTodos()[0].title).toBe('Buy milk');
  });

  it('persists to localStorage', () => {
    addTodo('Buy milk');
    const stored = JSON.parse(localStorage.getItem('playground.todos.v1') ?? '[]') as { title: string }[];
    expect(stored[0].title).toBe('Buy milk');
  });
});

describe('toggleTodo', () => {
  it('marks an incomplete todo as completed', () => {
    addTodo('Buy milk');
    const id = getTodos()[0].id;
    toggleTodo(id);
    expect(getTodos()[0].completed).toBe(true);
  });

  it('marks a completed todo back to incomplete', () => {
    addTodo('Buy milk');
    const id = getTodos()[0].id;
    toggleTodo(id);
    toggleTodo(id);
    expect(getTodos()[0].completed).toBe(false);
  });
});

describe('deleteTodo', () => {
  it('removes the todo with the given id', () => {
    addTodo('Buy milk');
    const id = getTodos()[0].id;
    deleteTodo(id);
    expect(getTodos()).toHaveLength(0);
  });

  it('leaves other todos intact', () => {
    addTodo('A');
    addTodo('B');
    const idA = getTodos()[0].id;
    deleteTodo(idA);
    expect(getTodos()).toHaveLength(1);
    expect(getTodos()[0].title).toBe('B');
  });
});

describe('subscribe', () => {
  it('calls listener with current todos when they change', () => {
    const listener = vi.fn();
    subscribe(listener);
    addTodo('Buy milk');
    expect(listener).toHaveBeenCalledWith(getTodos());
  });

  it('stops calling listener after unsubscribe', () => {
    const listener = vi.fn();
    const unsub = subscribe(listener);
    unsub();
    addTodo('Buy milk');
    expect(listener).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run tests to verify they fail (store.ts doesn't exist yet)**

```bash
pnpm --filter @playground/todo-app test
```

Expected: FAIL — `Cannot find module './store'`

- [ ] **Step 3: Implement store.ts**

Create `apps/todo-app/src/store.ts`:

```typescript
import type { Todo } from '@playground/types';

const STORAGE_KEY = 'playground.todos.v1';

function load(): Todo[] {
  try {
    return JSON.parse(localStorage.getItem(STORAGE_KEY) ?? '[]') as Todo[];
  } catch {
    return [];
  }
}

function save(items: Todo[]): void {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(items));
}

type Listener = (todos: Todo[]) => void;

const listeners = new Set<Listener>();
let todos: Todo[] = load();

export function getTodos(): Todo[] {
  return todos;
}

export function subscribe(listener: Listener): () => void {
  listeners.add(listener);
  return () => listeners.delete(listener);
}

function notify(): void {
  listeners.forEach(l => l(todos));
}

export function addTodo(title: string): void {
  const todo: Todo = {
    id: crypto.randomUUID(),
    title: title.trim(),
    completed: false,
  };
  todos = [...todos, todo];
  save(todos);
  notify();
}

export function toggleTodo(id: string): void {
  todos = todos.map(t => (t.id === id ? { ...t, completed: !t.completed } : t));
  save(todos);
  notify();
}

export function deleteTodo(id: string): void {
  todos = todos.filter(t => t.id !== id);
  save(todos);
  notify();
}

/** Resets in-memory state without touching localStorage. For tests only. */
export function _resetForTest(): void {
  todos = [];
  listeners.clear();
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
pnpm --filter @playground/todo-app test
```

Expected: all 8 tests PASS

- [ ] **Step 5: Commit**

```bash
git add apps/todo-app/src/store.ts apps/todo-app/src/store.test.ts
git commit -m "feat(todo-app): implement localStorage todo store with tests"
```

---

## Task 6: Implement todo-app UI components

**Files:**
- Create: `apps/todo-app/src/index.css`
- Create: `apps/todo-app/src/components/TodoInput.tsx`
- Create: `apps/todo-app/src/components/TodoList.tsx`
- Create: `apps/todo-app/src/components/TodoStats.tsx`
- Create: `apps/todo-app/src/App.tsx`
- Create: `apps/todo-app/src/remote-entry.tsx`

- [ ] **Step 1: Create apps/todo-app/src/index.css**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 2: Create apps/todo-app/src/components/TodoInput.tsx**

```tsx
import { useState, type FormEvent } from 'react';
import { addTodo } from '../store';

export function TodoInput() {
  const [value, setValue] = useState('');
  const [error, setError] = useState('');

  const onSubmit = (e: FormEvent) => {
    e.preventDefault();
    if (!value.trim()) {
      setError('Please enter a task.');
      return;
    }
    addTodo(value);
    setValue('');
    setError('');
  };

  return (
    <form onSubmit={onSubmit} className="space-y-1">
      <div className="flex gap-2">
        <input
          className="flex-1 bg-slate-800 border border-slate-700 rounded-md px-3 py-2 text-sm text-slate-100 placeholder-slate-500 focus:outline-none focus:ring-1 focus:ring-indigo-500"
          placeholder="Add a task..."
          value={value}
          onChange={e => setValue(e.target.value)}
          aria-label="todo title"
        />
        <button
          type="submit"
          className="px-4 py-2 bg-indigo-600 hover:bg-indigo-500 text-white text-sm font-medium rounded-md transition-colors"
        >
          Add
        </button>
      </div>
      {error && <p className="text-red-400 text-xs">{error}</p>}
    </form>
  );
}
```

- [ ] **Step 3: Create apps/todo-app/src/components/TodoList.tsx**

```tsx
import { useSyncExternalStore } from 'react';
import { getTodos, subscribe, toggleTodo, deleteTodo } from '../store';

export function TodoList() {
  const todos = useSyncExternalStore(subscribe, getTodos, getTodos);

  if (todos.length === 0) {
    return <p className="text-slate-500 text-sm">No tasks yet. Add one above.</p>;
  }

  return (
    <ul className="space-y-1.5">
      {todos.map(todo => (
        <li key={todo.id} className="flex items-center gap-3 group">
          <input
            type="checkbox"
            checked={todo.completed}
            onChange={() => toggleTodo(todo.id)}
            className="h-4 w-4 rounded border-slate-600 bg-slate-800 accent-indigo-500"
            aria-label={`Toggle ${todo.title}`}
          />
          <span
            className={`flex-1 text-sm ${
              todo.completed ? 'line-through text-slate-500' : 'text-slate-100'
            }`}
          >
            {todo.title}
          </span>
          <button
            onClick={() => deleteTodo(todo.id)}
            className="text-slate-600 hover:text-red-400 opacity-0 group-hover:opacity-100 transition-opacity text-xs"
            aria-label={`Delete ${todo.title}`}
          >
            ✕
          </button>
        </li>
      ))}
    </ul>
  );
}
```

- [ ] **Step 4: Create apps/todo-app/src/components/TodoStats.tsx**

```tsx
import { useSyncExternalStore } from 'react';
import { getTodos, subscribe } from '../store';

export function TodoStats() {
  const todos = useSyncExternalStore(subscribe, getTodos, getTodos);
  const total = todos.length;
  const done = todos.filter(t => t.completed).length;
  const remaining = total - done;

  return (
    <div className="flex gap-6 text-xs text-slate-500 border-t border-slate-800 pt-3">
      <span>
        <span className="text-slate-300 font-medium">{total}</span> total
      </span>
      <span>
        <span className="text-slate-300 font-medium">{done}</span> done
      </span>
      <span>
        <span className="text-slate-300 font-medium">{remaining}</span> remaining
      </span>
    </div>
  );
}
```

- [ ] **Step 5: Create apps/todo-app/src/App.tsx**

```tsx
import { TodoInput } from './components/TodoInput';
import { TodoList } from './components/TodoList';
import { TodoStats } from './components/TodoStats';
import './index.css';

export function App() {
  return (
    <div className="space-y-4 p-4">
      <TodoInput />
      <TodoList />
      <TodoStats />
    </div>
  );
}
```

- [ ] **Step 6: Create apps/todo-app/src/remote-entry.tsx**

```tsx
import { createRoot, type Root } from 'react-dom/client';
import { App } from './App';

export function mount(target: HTMLElement): () => void {
  const root: Root = createRoot(target);
  root.render(<App />);
  return () => root.unmount();
}
```

- [ ] **Step 7: Verify the MFE builds**

```bash
pnpm --filter @playground/todo-app build
```

Expected: `dist/remoteEntry.js` created, no errors

- [ ] **Step 8: Commit**

```bash
git add apps/todo-app/src/
git commit -m "feat(todo-app): implement UI components (input, list, stats) with Tailwind"
```

---

## Task 7: Set up Tailwind + shadcn on host

**Files:**
- Create: `apps/host/tailwind.config.ts`
- Create: `apps/host/postcss.config.cjs`
- Create: `apps/host/components.json`
- Create: `apps/host/src/lib/utils.ts`
- Create: `apps/host/src/components/ui/` (via shadcn CLI)

- [ ] **Step 1: Create apps/host/tailwind.config.ts**

```typescript
import type { Config } from 'tailwindcss';

export default {
  content: [
    './src/pages/**/*.{ts,tsx}',
    './src/components/**/*.{ts,tsx}',
    './src/app/**/*.{ts,tsx}',
  ],
  darkMode: 'class',
  theme: {
    extend: {},
  },
  plugins: [
    require('@tailwindcss/typography'),
  ],
} satisfies Config;
```

- [ ] **Step 2: Create apps/host/postcss.config.cjs**

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 3: Create apps/host/components.json (shadcn config)**

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/app/globals.css",
    "baseColor": "slate",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

- [ ] **Step 4: Create apps/host/src/lib/utils.ts**

```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 5: Install shadcn components (installs peer deps automatically)**

```bash
cd apps/host
pnpm dlx shadcn@latest add button command separator --yes
cd ../..
```

Expected: `src/components/ui/button.tsx`, `src/components/ui/command.tsx`, `src/components/ui/separator.tsx` created; `clsx`, `tailwind-merge`, `cmdk`, etc. added to package.json

- [ ] **Step 6: Replace apps/host/src/app/globals.css with Tailwind + shadcn dark theme**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --card: 222.2 84% 4.9%;
    --card-foreground: 210 40% 98%;
    --popover: 222.2 84% 4.9%;
    --popover-foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
    --secondary: 217.2 32.6% 17.5%;
    --secondary-foreground: 210 40% 98%;
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
    --radius: 0.5rem;
  }

  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --card: 222.2 84% 4.9%;
    --card-foreground: 210 40% 98%;
    --popover: 222.2 84% 4.9%;
    --popover-foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
    --secondary: 217.2 32.6% 17.5%;
    --secondary-foreground: 210 40% 98%;
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

- [ ] **Step 7: Run pnpm install from root to sync lockfile**

```bash
pnpm install
```

- [ ] **Step 8: Commit**

```bash
git add apps/host/tailwind.config.ts apps/host/postcss.config.cjs apps/host/components.json apps/host/src/lib/ apps/host/src/components/ui/ apps/host/src/app/globals.css apps/host/package.json pnpm-lock.yaml
git commit -m "feat(host): add Tailwind + shadcn/ui (button, command, separator)"
```

---

## Task 8: Rewrite host shell — layout, header, sidebar, error page

**Files:**
- Modify: `apps/host/src/app/layout.tsx`
- Modify: `apps/host/src/app/error.tsx`
- Create: `apps/host/src/components/header.tsx`
- Create: `apps/host/src/components/sidebar.tsx`

- [ ] **Step 1: Replace apps/host/src/app/layout.tsx**

```typescript
import type { Metadata } from 'next';
import './globals.css';
import { Header } from '@/components/header';
import { Sidebar } from '@/components/sidebar';

export const metadata: Metadata = {
  title: 'playground',
  description: 'A monorepo playground for experimenting with multi-agent Claude Code workflows.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark">
      <body className="bg-slate-950 text-slate-100 antialiased flex flex-col h-screen overflow-hidden">
        <Header />
        <div className="flex flex-1 overflow-hidden">
          <Sidebar />
          <main className="flex-1 overflow-auto">
            {children}
          </main>
        </div>
      </body>
    </html>
  );
}
```

- [ ] **Step 2: Create apps/host/src/components/header.tsx**

```typescript
'use client';

import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import {
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
} from '@/components/ui/command';

const apps = [
  { href: '/todo', label: 'Todo' },
  { href: '/readme', label: 'README' },
] as const;

export function Header() {
  const [open, setOpen] = useState(false);
  const router = useRouter();

  useEffect(() => {
    const onKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'k' && (e.metaKey || e.ctrlKey)) {
        e.preventDefault();
        setOpen(o => !o);
      }
    };
    document.addEventListener('keydown', onKeyDown);
    return () => document.removeEventListener('keydown', onKeyDown);
  }, []);

  return (
    <header className="h-12 bg-slate-900 border-b border-slate-800 flex items-center justify-between px-4 shrink-0">
      <span className="text-indigo-400 font-bold tracking-tight text-sm">playground</span>

      <button
        onClick={() => setOpen(true)}
        className="flex items-center gap-2 bg-slate-800 hover:bg-slate-700 text-slate-400 text-sm px-3 py-1.5 rounded-md border border-slate-700 transition-colors"
      >
        <span>Search apps...</span>
        <kbd className="text-xs text-slate-500 font-mono bg-slate-900 px-1.5 py-0.5 rounded border border-slate-700">
          ⌘K
        </kbd>
      </button>

      <a
        href="https://github.com/mortenbroesby/playground"
        target="_blank"
        rel="noopener noreferrer"
        className="text-slate-400 hover:text-slate-100 text-sm transition-colors"
      >
        GitHub ↗
      </a>

      <CommandDialog open={open} onOpenChange={setOpen}>
        <CommandInput placeholder="Search apps..." />
        <CommandList>
          <CommandEmpty>No apps found.</CommandEmpty>
          <CommandGroup heading="Apps">
            {apps.map(app => (
              <CommandItem
                key={app.href}
                onSelect={() => {
                  router.push(app.href);
                  setOpen(false);
                }}
              >
                {app.label}
              </CommandItem>
            ))}
          </CommandGroup>
        </CommandList>
      </CommandDialog>
    </header>
  );
}
```

- [ ] **Step 3: Create apps/host/src/components/sidebar.tsx**

```typescript
'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { cn } from '@/lib/utils';

const apps = [
  { href: '/todo', label: 'Todo' },
  { href: '/readme', label: 'README' },
] as const;

export function Sidebar() {
  const pathname = usePathname();

  return (
    <aside className="w-48 bg-slate-900 border-r border-slate-800 p-3 flex-shrink-0">
      <p className="text-xs text-slate-500 uppercase tracking-wider mb-2 px-1">Apps</p>
      <nav className="space-y-0.5">
        {apps.map(app => (
          <Link
            key={app.href}
            href={app.href}
            className={cn(
              'block px-3 py-2 rounded text-sm transition-colors',
              pathname === app.href
                ? 'bg-slate-800 text-slate-100 border-l-2 border-indigo-500'
                : 'text-slate-400 hover:text-slate-100 hover:bg-slate-800/50'
            )}
          >
            {app.label}
          </Link>
        ))}
      </nav>
    </aside>
  );
}
```

- [ ] **Step 4: Replace apps/host/src/app/error.tsx (remove Mantine)**

```typescript
'use client';

type ErrorPageProps = {
  error: Error & { digest?: string };
  reset: () => void;
};

export default function ErrorPage({ error, reset }: ErrorPageProps) {
  return (
    <div className="p-6 space-y-4">
      <h2 className="text-lg font-semibold text-slate-100">Something went wrong</h2>
      <p className="text-sm text-red-400 bg-red-950/30 border border-red-900 rounded-md px-4 py-3">
        {error.message}
      </p>
      <button
        onClick={reset}
        className="px-4 py-2 bg-slate-800 hover:bg-slate-700 text-slate-100 text-sm rounded-md border border-slate-700 transition-colors"
      >
        Try again
      </button>
    </div>
  );
}
```

- [ ] **Step 5: Verify TypeScript compiles**

```bash
pnpm --filter @playground/host type-check
```

Expected: no errors (Mantine types gone, shadcn types in place)

- [ ] **Step 6: Commit**

```bash
git add apps/host/src/app/layout.tsx apps/host/src/app/error.tsx apps/host/src/components/header.tsx apps/host/src/components/sidebar.tsx
git commit -m "feat(host): rewrite shell with dark layout, header, and sidebar"
```

---

## Task 9: Add /todo route with MFE frame

**Files:**
- Create: `apps/host/src/components/mfe-frame.tsx`
- Create: `apps/host/src/app/todo/page.tsx`

- [ ] **Step 1: Create apps/host/src/components/mfe-frame.tsx**

```typescript
'use client';

import { useEffect, useRef } from 'react';

type RemoteMount = (container: HTMLElement) => () => void;

export function MfeFrame({ remoteUrl }: { remoteUrl: string }) {
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (!containerRef.current) return;
    const container = containerRef.current;
    let cleanup: (() => void) | undefined;

    // webpackIgnore: true tells Next.js bundler to leave this as a native
    // browser dynamic import, so the browser resolves the external URL at runtime.
    import(/* webpackIgnore: true */ remoteUrl)
      .then((module: { mount: RemoteMount }) => {
        if (container) {
          cleanup = module.mount(container);
        }
      })
      .catch(() => {
        if (container) {
          container.innerHTML =
            '<p class="text-slate-500 text-sm p-4">MFE unavailable — start todo-app dev server on port 3101.</p>';
        }
      });

    return () => cleanup?.();
  }, [remoteUrl]);

  return <div ref={containerRef} className="w-full" />;
}
```

- [ ] **Step 2: Create apps/host/src/app/todo/page.tsx**

```typescript
import { MfeFrame } from '@/components/mfe-frame';

export default function TodoPage() {
  return (
    <div className="p-6">
      <h1 className="text-slate-400 text-xs uppercase tracking-wider mb-4">Todo</h1>
      <MfeFrame remoteUrl="http://localhost:3101/remoteEntry.js" />
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add apps/host/src/components/mfe-frame.tsx apps/host/src/app/todo/
git commit -m "feat(host): add /todo route with runtime MFE frame"
```

---

## Task 10: Add /readme route

**Files:**
- Create: `apps/host/src/app/readme/page.tsx`

- [ ] **Step 1: Create apps/host/src/app/readme/page.tsx**

```typescript
import { readFileSync } from 'fs';
import { join } from 'path';
import ReactMarkdown from 'react-markdown';
import rehypeHighlight from 'rehype-highlight';
import 'highlight.js/styles/github-dark.css';

export default function ReadmePage() {
  // process.cwd() in Next.js = apps/host directory
  // ../../README.md = monorepo root README
  const readme = readFileSync(join(process.cwd(), '../../README.md'), 'utf-8');

  return (
    <div className="max-w-4xl mx-auto px-6 py-8">
      <h1 className="text-slate-400 text-xs uppercase tracking-wider mb-6">README</h1>
      <article className="prose prose-invert prose-slate max-w-none">
        <ReactMarkdown rehypePlugins={[rehypeHighlight]}>
          {readme}
        </ReactMarkdown>
      </article>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add apps/host/src/app/readme/
git commit -m "feat(host): add /readme route rendering monorepo README as styled markdown"
```

---

## Task 11: Update page.tsx, delete old host files, verify full build

**Files:**
- Modify: `apps/host/src/app/page.tsx`
- Delete: `apps/host/src/components/remote-slot.tsx`
- Delete: `apps/host/src/components/todo-debug-panel.tsx`
- Delete: `apps/host/src/components/todo-orchestrator.tsx`
- Delete: `apps/host/src/lib/todo-bridge.ts`
- Delete: `apps/host/src/lib/injected-remotes.ts`
- Delete: `apps/host/src/lib/remotes.ts`

- [ ] **Step 1: Replace apps/host/src/app/page.tsx with a redirect**

```typescript
import { redirect } from 'next/navigation';

export default function HomePage() {
  redirect('/todo');
}
```

- [ ] **Step 2: Delete old host files**

```bash
rm apps/host/src/components/remote-slot.tsx
rm apps/host/src/components/todo-debug-panel.tsx
rm apps/host/src/components/todo-orchestrator.tsx
rm apps/host/src/lib/todo-bridge.ts
rm apps/host/src/lib/injected-remotes.ts
rm apps/host/src/lib/remotes.ts
```

- [ ] **Step 3: Verify host TypeScript is clean**

```bash
pnpm --filter @playground/host type-check
```

Expected: 0 errors

- [ ] **Step 4: Build the host**

```bash
pnpm --filter @playground/host build
```

Expected: Next.js build succeeds, pages `/`, `/todo`, `/readme` all listed in output

- [ ] **Step 5: Build todo-app**

```bash
pnpm --filter @playground/todo-app build
```

Expected: `dist/remoteEntry.js` created

- [ ] **Step 6: Run the full Turbo build**

```bash
pnpm turbo build
```

Expected: all workspaces build successfully, no errors

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: complete microfrontend cleanup — dark shell, Tailwind/shadcn, todo-app MFE, README viewer"
```

---

## Smoke Test (manual, after build)

Run both dev servers in two terminals:

**Terminal 1 — todo-app MFE:**
```bash
pnpm --filter @playground/todo-app dev
```
Expected: preview server at `http://localhost:3101`

**Terminal 2 — host shell:**
```bash
pnpm --filter @playground/host dev
```
Expected: Next.js at `http://localhost:3000`

Open `http://localhost:3000`:
- Redirects to `/todo` ✓
- Dark shell: header with "playground" wordmark, search button, GitHub link ✓
- Sidebar shows "Todo" (active) and "README" ✓
- TodoApp MFE renders with input bar, empty list, stats ✓
- Add a task: appears in list, stats update ✓
- Toggle a task: strikes through, done count increments ✓
- Delete a task: removed from list ✓
- Navigate to `/readme`: README renders as styled markdown ✓
- Press ⌘K: command palette opens, apps searchable ✓
- Reload page: todos persist from localStorage ✓
- No Mantine anywhere in Network tab ✓
