# @playground/todo-stats

Todo stats micro frontend exposing a runtime `mount` entrypoint at `remoteEntry.js`.

In local `dev`, this app now runs a watch build and serves the compiled `remoteEntry.js` so the host can load it from the configured runtime URL.
