module.exports = {
  allowDirectMainPush: true,
  // One path per line. Lines starting with # are ignored.
  // Use exact repo-relative paths, or a directory prefix ending in `/**`.
  //
  // Add entries here only while intentionally refactoring shared agent
  // infrastructure such as hooks or Claude hook settings.
  infrastructureEditAllowlist: [],
};
